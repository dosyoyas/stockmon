# ==============================================================================
# StockMon Environment Variables Example
# ==============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control (already in .gitignore)

# ==============================================================================
# API Configuration (Required for both API server and Client)
# ==============================================================================

# API Key for authentication
# Used by: API server to validate incoming requests, Client to authenticate with API
# Generate a strong random key: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=your-secret-api-key-here

# API Server Port (Optional)
# Default: 8000
# Railway deployment: PORT is automatically set by Railway, do not override
# Local development: uncomment and modify if needed
# PORT=8000

# ==============================================================================
# Client Configuration (Required only on Raspberry Pi)
# ==============================================================================

# API URL Override (Optional)
# Default: Uses api_url from client/config.json
# Use this to override the API URL for local testing
# Example for local development: API_URL=http://localhost:8000/check-alerts
# API_URL=https://your-api.railway.app/check-alerts

# SMTP Configuration for Email Notifications
# Required for sending alert emails from the client
# Example Gmail configuration (requires App Password, not regular password):
#   1. Enable 2FA on your Google account
#   2. Generate App Password: https://myaccount.google.com/apppasswords
#   3. Use the 16-character app password below

# SMTP server hostname
SMTP_HOST=smtp.gmail.com

# SMTP username (usually your email address)
SMTP_USER=your-email@gmail.com

# SMTP password (use App Password for Gmail, not your regular password)
SMTP_PASS=your-app-password-here

# Destination email for stock alerts
NOTIFY_EMAIL=recipient@example.com
